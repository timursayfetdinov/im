import { type EdgeProps } from '@xyflow/react';
import type { DiagramEdgeData } from '../lib/buildDiagramGraph';
export declare function DiagramEdge({ id, sourceX, sourceY, targetX, targetY, sourcePosition, targetPosition, style, label, labelStyle, labelBgStyle, data, }: EdgeProps<DiagramEdgeData>): import("react/jsx-runtime").JSX.Element;
