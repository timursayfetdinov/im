/**
 * Как показывать путь по шагам в превью-сценарии.
 * Чтобы вернуть хлебные крошки: замените на `'breadcrumbs'`.
 */
export type PlayerPathLayoutMode = 'timeline' | 'breadcrumbs';

export const PLAYER_PATH_LAYOUT_MODE: PlayerPathLayoutMode = 'timeline';
